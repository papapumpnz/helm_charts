apiVersion: v1
kind: Service
metadata:
  name: {{ include "financefeast_customer.fullname" . }}
  labels:
    {{- include "financefeast_customer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "financefeast_customer.selectorLabels" . | nindent 4 }}

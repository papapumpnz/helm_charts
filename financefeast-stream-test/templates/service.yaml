apiVersion: v1
kind: Service
metadata:
  name: {{ include "financefeast-stream-test.fullname" . }}
  labels:
    {{- include "financefeast-stream-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "financefeast-stream-test.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker_registry.fullname" . }}
  labels:
    {{- include "docker_registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docker_registry.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docker_registry.fullname" . }}-test-connection"
  labels:
    {{- include "docker_registry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docker_registry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
